# 交接文档：Palink-AI 角色变量(MVU)与对话故障排查

> 交接对象：其他 agent / 后续维护者
> 整理时间：2026-08-18（后端容器 `palink-ai-backend-1` 已用全部修复重建，运行 ~19 分钟，healthy）
> 整理人：前序排查 agent（Craft 模式）
> 项目路径：`D:\项目\Palink-AI`
> 数据库：`postgresql://ai_user:ai_password@db:5432/ai_hub`（在 `palink-ai-db-1` 容器内可直连）

---

## 0. 一句话结论（⚠️ 与最初假设不同，已用实测推翻）

1. **世界书 `UniqueViolation` 崩溃 —— 已修复、已部署、已实测生效。**
   新会话可连续多轮（实测 3 轮）正常生成，后端日志近 20 分钟内**无任何 `UniqueViolation`**。
   最初"修复没生效/第二局仍崩"的判断是错的：容器在 19 分钟前用含修复的代码重建过，崩溃确实消失了。

2. **变量(MVU)输出 —— 仍未解决，且根因已转移。**
   不再是代码崩溃，而是**模型不遵从指令**：MVU 指令确实注入到了 prompt，但 `deepseek-v4-flash` 不输出 `<UpdateVariable>` 标签。
   实测会话 `1b955bdd-...` 的 3 条 assistant 消息**全部不含 `<UpdateVariable>`**。

3. **强烈怀疑的元凶（高概率，可直接修）：**
   - 系统提示词全局规则 `禁止使用：<action>、<thinking>等XML标签`（`backend/app/core/default_prompts.py:79/93`）。
     MVU 用户尾部指令只是在**用户消息**里声明"此标签不受禁止 XML 规则限制"，但 deepseek 更服从**系统级**禁令，于是把 XML 风格的 `<UpdateVariable>` 一起压掉了。
   - 叠加 `thinking` 泄漏：deepseek `reasoning_effort=None` → 思考强制开启，模型把 `<think>` 块直接写进**正文**，与尾部结构化标签争夺输出。

---

## 1. 已尝试的方案（按时间线，含成功/失败）

| # | 轮次 | 动作 | 文件/位置 | 结果 |
|---|------|------|-----------|------|
| 1 | R1 | 诊断"卡片无变量信息"：发现 MVU 指令埋在世界书拼接消息中部 @8813，之后还有 ~4.7KB 场景描述，模型注意力覆盖不到 | `roleplay_prompt_assembly.py` | 定位可见性根因 |
| 2 | R1 | 把 MVU 变量指令以 **user tail** 形式贴到最近一条 user 消息末尾（推理模型对最近 user 遵循度最高） | `roleplay_prompt_assembly.py:3986-4020`（`[MVU]` 注入块） | 指令可见性提升，但模型仍不稳定输出 |
| 3 | R2 | 误判第二次失败为"模型 API 随机故障"；加：**Error 文本不入库** + **空响应自动重试 1 次** + **只有思考无正文也判失败** + **历史装配过滤已入库 Error 消息** + **重试指数退避 4s→8s** | `websocket.py`(`persist_snapshot`/`EMPTY-RESP-RETRY`)、`character_ext.py`(历史去重后过滤 Error) | 打破"Error 入库→循环污染"，但未触及真正根因 |
| 4 | R3 | 加强 MVU 指令"必须完整输出角色字段（好感度等）"；`macro_service.py` 加**严格禁止把日期/天气/数值抄进正文**文案 | `roleplay_prompt_assembly.py:4005-4008`、`macro_service.py:_format_status_current_variable_ref` | 缓解泄露，未解决不输出 |
| 5 | R4 | 发现思维链异常重复一个词（"666"卡死）——这是世界书注入失败后的症状 | 日志 `2190` | 指向世界书链路 |
| 6 | R4/R5 | **实锤真正根因**：世界书 `TimedEffectsManager.update_after_message()` 把场景条目过期状态行 `db.delete`，紧接着 `record_activation` 对同一 `(session_id, entry_id)` 重新 `insert`；同事务内 SQLAlchemy flush 顺序为 insert 先于 delete → 撞未删旧行 → `UniqueViolation` → `build_worldbook_context` 抛异常 → **整个世界书被跳过** → 提示词缺世界观/变量指令 → 模型空响应/思维链重复/变量不全。**第一次对话 DB 无状态行不冲突，第二次起必冲突。** | `worldbook_service.py:252-284`（`[WB-STATE-FIX]`） | **已修复：过期状态行不再删除（保留无害）** |
| 7 | R5 | 验证修复：探针显示修复前 9 insert+20 update（冲突），修复后 0 insert+29 update 零冲突；生产装配验证世界书正常注入（10903 字符） | 诊断脚本 `_probe_wb_activation.py` / `_repro_wb_unique.py` | 源代码层验证通过 |
| 8 | 今天 | **重新核实部署**：`docker exec` 进运行容器确认 `[WB-STATE-FIX]` 存在（count=1）；查 `character_chat_messages` 确认新会话多轮成功；确认 MVU 指令注入代码与卡片 gate 均生效 | 见 §2 | 确认崩溃已消失、问题已转移 |

> 注意：R6 的修复是**源代码层**验证（探针直接跑源码）。R8 才确认它**已进运行容器**——这是最初误判"修复无效"的关键：探针通过 ≠ 容器生效，必须 `docker compose build backend && up -d backend` 后验证运行容器。

---

## 2. 当前真实表现（数据库 + 日志实测，非推测）

**关键实测会话**：`1b955bdd-d836-4d1d-8827-792e3ee4d884`（角色卡 `93747c75-d0fa-4e6d-8179-9a649e8f7d9b`「我被猫娘包围了！」）

数据库实查（`character_chat_messages`）：
```
2191  assistant  575  无<UpdateVariable>  无Error  首条回复（猫神开场）
2192  user       2    "开始"
2193  assistant  1660 无<UpdateVariable>  以<think>开头（thinking 泄漏进正文）
2194  user       2    "开始"
2195  assistant  1662 无<UpdateVariable>  以<think>开头
2196  user       3    "666"
```
- ✅ **3 轮 user/assistant 全部成功生成**，无崩溃、无 `UniqueViolation`。→ 世界书崩溃修复**确已生效**。
- ❌ **3 条 assistant 消息均不含 `<UpdateVariable>`**。→ MVU 变量输出**仍完全失败**。
- ⚠️ 消息 `2193/2195` 以 `<think>` 开头，说明模型把思考块写进了 content 正文（thinking 未分离）。

后端日志（`docker logs --since 20m`）：
- `model=deepseek-v4-flash max_tokens=16384 temperature=0.70 enable_thinking=None reasoning_effort=None`
- 全部 `HTTP/1.1 200 OK`，无 `World book context injection failed`。
- `is_deepseek and reasoning_effort is None` → `reasoning_on=True`（`inference_dispatcher.py:352/681`）→ 思考强制开启。

卡片 gate 实测（`_card_has_mvu_scripts`）：
- 该卡 `extensions` 含 `regex_scripts`，其中一条 `findRegex = /<UpdateVariable>(.*?)<\/UpdateVariable>/gis`
- → gate 返回 `True` → **MVU user-tail 指令确实被注入到每条 prompt 的最后一条 user 消息**

**结论**：指令已到达模型，模型不遵从。问题从"代码崩溃"转为"模型遵从度 + 指令冲突"。

---

## 3. 可能原因（按概率排序，给接手 agent 优先级）

1. **【高】系统提示词全局「禁止 XML 标签」压制 `<UpdateVariable>`**
   - 证据：`default_prompts.py:79` `禁止使用：<action>、<thinking>等XML标签` 与 `:93` 英文版 `Prohibited: XML tags like <action> or <thinking>`。
   - MVU 用户尾部指令只说"此标签不受禁止 XML 规则限制"，但这是**用户消息**层声明，deepseek 对**系统级**禁令服从度更高，把 XML 风格的 `<UpdateVariable>`（及 `<JSONPatch>`）一并抑制。
   - **可直接修**：在该规则中显式放行 `<UpdateVariable>`/`<StatusPlaceHolderImpl>`，或在系统提示词以**同等权威**补充 carve-out。

2. **【高】deepseek-v4-flash 对"XML 内嵌 JSON Patch"结构遵从度弱**
   - 即便指令可见，模型（尤其中文 roleplay 长文 + 思考模式）常省略尾部结构化标签。
   - 验证方向：用最小 prompt（仅 MVU user-tail + 短场景）单独测，关闭思考看是否出现。

3. **【中】thinking 泄漏挤占输出**
   - 思考块进入正文（见 §2），可能消耗输出预算 / 注意力，导致尾部 `<UpdateVariable>` 被丢弃。
   - 日志确认 `reasoning_effort=None` 对 deepseek 即开启思考。

4. **【中】指令位置/注意力衰减**
   - 即便在 user tail，长 roleplay 回复后模型未必可靠地"在**最末尾**追加结构化块"。

5. **【低】max_tokens 截断**
   - 当前样本（1660 字符）远未触顶 16384，排除本次；但长回复下仍可能。

---

## 4. 连带 / 下游问题

- **旧会话残留脏数据**：历史会话可能含 Error / 空响应消息行。`character_ext.py` 已在装配时过滤，但 **DB 行未清理**，长期积累污染检索/统计。建议脚本批量标记或删除 `content` 为 Error 占位符的消息。
- **`<think>` 泄漏到正文**：前端 status / 格式化可能误处理思考块；且若前端依赖"正文不含 think"则显示异常。
- **`[MVU-SKIP]` 副作用**：MVU 卡不注入任何 `<status>` 指令（`status_bar_detector.py:382-384`）。面板若依赖 `<status>` 会断——但 MVU 卡走 `<StatusPlaceHolderImpl/>` 占位符，理论上 OK，需回归确认面板正常。
- **模型网关间歇空响应**：opencode.ai `deepseek-v4-flash` 存在 HTTP 200 但内容为空的情况；已有重试+退避，但非 100% 覆盖（极端情况仍可能空响应）。
- **变量泄露风险**：即便修了 XML 禁令，若模型仍把日期/天气/数值抄进正文，`macro_service.py` 的禁止文案与面板可能双重渲染。

---

## 5. 给接手 agent 的下一步（可复制命令）

**A. 先确认崩溃确已消失（用户侧回归）**
```
# 让用户开新卡连聊 3 轮；后台盯日志
"C:/Program Files/Docker/Docker/resources/bin/docker.exe" logs -f palink-ai-backend-1 | grep -i "UniqueViolation\|World book context injection failed"
# 期望：无输出
```

**B. 修 XML 禁令冲突（最优先，最可能直接解决）**
- 改 `backend/app/core/default_prompts.py` 的 `OUTPUT_FORMAT_TEMPLATE_ZH/EN`（:79 / :93）：
  把 `禁止使用：<action>、<thinking>等XML标签`
  改为 `禁止使用：<action>、<thinking>等XML标签（但 <UpdateVariable> 与 <StatusPlaceHolderImpl> 例外，必须输出）`
- 重建后端：`docker compose build backend && docker compose up -d backend`
- 重建后验证运行容器含改动：`docker exec palink-ai-backend-1 grep -n "UpdateVariable" /app/app/core/default_prompts.py`

**C. 实测模型遵从度（隔离思考因素）**
```
# 最小复现：只给 MVU user-tail + 一句话场景，reasoning 关，看是否出 <UpdateVariable>
cd backend && python scripts/_repro_model_call.py   # 改 SESSION_ID/消息；设 reasoning_effort="off"
```
- 若 `reasoning_effort=off` 时**能**输出、`=auto/None` 时**不能** → 确认 thinking 干扰，需对 MVU 卡强制关思考或把 `<think>` 从 content 剥离。

**D. 加服务端兜底（推荐，降低对模型遵从度的依赖）**
- `mvu_engine.py` 已存在（见 git status 已修改）。研究是否能在模型返回后：
  (a) 若正文无 `<UpdateVariable>` 但卡是 MVU → 二次提示强制补齐；或
  (b) 用轻量启发式从正文抽取变量变化自动生成 patch。
- 这能从根本上解除"模型不输出→面板恒为初始值"的死结。

**E. 清理脏历史数据（低优）**
```
docker exec palink-ai-db-1 psql -U ai_user -d ai_hub -c "SELECT id, session_id FROM character_chat_messages WHERE content LIKE '%<Error%' OR content LIKE 'Error:%' LIMIT 50;"
# 确认后按需软删/标记
```

---

## 6. 关键文件 / 函数 / 行号速查

| 文件 | 位置 | 作用 / 状态 |
|------|------|------------|
| `backend/app/services/worldbook_service.py` | `update_after_message()` :252-284 | **`[WB-STATE-FIX]` 已修复**：过期状态行不再 delete（解决 UniqueViolation） |
| `backend/app/services/roleplay_prompt_assembly.py` | :3986-4020 | MVU user-tail 指令注入（gate 通过后贴到最后一条 user 消息） |
| `backend/app/core/default_prompts.py` | :79 / :93 | **⚠️ 全局「禁止 XML 标签」——疑似压制 `<UpdateVariable>` 的元凶** |
| `backend/app/services/status_bar_detector.py` | `_card_has_mvu_scripts` :347-369 | MVU 卡 gate；该卡 findRegex 含 `/<UpdateVariable>.../gis` → 返回 True |
| `backend/app/services/inference_dispatcher.py` | :344-370 / :673-698 | 思考开关：deepseek + `reasoning_effort=None` → 思考强制开 |
| `backend/app/services/stream_builder.py` | :123-125 | `enable_thinking=False` 时 reasoning 合并进 content（本次 enable_thinking=None，思考未分离 → 泄漏） |
| `backend/app/api/websocket.py` | `persist_snapshot` / `EMPTY-RESP-RETRY` | Error 不入库 + 空响应重试 + 只有思考无正文判失败 |
| `backend/app/api/character_ext.py` | 历史装配 | 去重后过滤已入库 Error 消息 |
| `backend/app/services/macro_service.py` | `_format_status_current_variable_ref` | 严格禁止把日期/天气/数值抄进正文 |

---

## 7. 复现 / 验证脚本清单（均在 `backend/scripts/`，未提交）

| 脚本 | 用途 |
|------|------|
| `_verify_prompt_assembly.py` / `_verify_prompt2/3/4.py` | 模拟提示词装配，统计 messages 长度/位置/变量注入 |
| `_repro_model_call.py` | 用真实提示词直连模型 API 复现（可改 `max_tokens`/`reasoning_effort`） |
| `_test_model_stability.py` | 连续调用测空响应率（此前 8/8 正常） |
| `_repro_wb_unique.py` / `_repro_wb_twice.py` / `_diag_wb_states.py` / `_probe_wb_activation.py` | 复现/探针世界书 UniqueViolation，统计 insert/update 分布 |
| `_sim_mvu_tail.py` / `_test_mvu_bare.py` / `_test_mvu_macro.py` / `_verify_wb_mvu.py` | MVU 指令注入与宏相关验证 |

**数据库快速查询（在 `palink-ai-db-1` 容器内）：**
```sql
-- 最近会话及消息数
SELECT session_id, COUNT(*) n,
  SUM(CASE WHEN role='user' THEN 1 ELSE 0 END) u,
  SUM(CASE WHEN role='assistant' THEN 1 ELSE 0 END) a
FROM character_chat_messages GROUP BY session_id ORDER BY MAX(id) DESC LIMIT 8;

-- 某会话 assistant 消息是否含 <UpdateVariable> / Error
SELECT id, role, length(content) clen,
  (content LIKE '%<UpdateVariable>%') has_mvu,
  (content LIKE '%Error%') has_err,
  left(content,120) head
FROM character_chat_messages WHERE session_id='<SID>' ORDER BY id;

-- 卡片是否过 MVU gate（看 regex_scripts.findRegex 是否含 UpdateVariable）
SELECT js->>'findRegex' FROM characters,
  jsonb_array_elements(extensions::jsonb->'regex_scripts') js
WHERE id='<CHAR_ID>' AND js->>'findRegex' LIKE '%UpdateVariable%';
```

---

## 8. 给接手 agent 的提醒（避坑）

- **不要只靠探针/源码验证就宣布修复**——必须 `docker compose build backend && up -d backend` 后 `docker exec` 进运行容器确认代码确实在跑（本次就因未确认部署而误判"修复无效"）。
- **git 重型操作在本环境会被进程级 SIGKILL**（见用户记忆）：禁止 `git stash -u` / `fetch` / `clone` / 整树 `tar`。备份用 PowerShell `Copy-Item`。
- 当前分支有大量未提交修改（见 `git status`），提交前请先与用户确认范围。
- 模型网关走 opencode.ai 代理，命令行需 `export http_proxy=https_proxy=http://127.0.0.1:7897` 才能访问 GitHub。
