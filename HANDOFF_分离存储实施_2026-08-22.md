# HANDOFF — 思考/正文分离存储实施（2026-08-22 立项交接）

> 接手者：下一个专注会话的 agent。**本任务是独立立项**，动工前必读：本文 + `TODOS.md` 对应条目 + `.trae/specs/separate-reasoning-pipeline/tasks.md` 底部架构决策存档。
> 预估工作量：一个完整会话（后端半天 / 前端+审计约一天当量）。

---

## 0. 任务目标（一句话）

把 assistant 消息的存储从「`<think>`思维链``+正文 混合体」迁移为「content=纯正文 + extra.reasoning=思考」的 ST 式分离存储，**前端 8 处消费点与存量数据一体迁移**。

## 1. 为什么之前不做、现在做

- 2026-08-22 首次评估时以为内联格式是脏数据，复核发现它是前后端契约（前端主动拼接），故搁置
- 用户后续确认：省空间收益（典型推理回复省约 60% 消息体积，思维链越长省越多）值得专项投入
- **插件影响审计已完成**（结论见 §3）：风险有界、缓冲点明确

## 2. 现状架构（改动前，全部已实证）

```
写入: websocket.py L651 / character_ext.py L4580
      final = "<think>" + regexed_reasoning + "</think>\n" + 正文
      msg_extra["reasoning"] = regexed_reasoning   ← 双写第二份
读取:
  ├─ 自研 React 显示: formatting.ts L950 extractReasoningTags 实时提取剥离（补丁性代码）
  ├─ 插件 runtime.chat[i].mes: 原始混合体（未剥离）← 唯一风险点
  └─ prompt 注入: builder strip_inline_think（已统一化）/ 记忆: clean_memory_content
存量: 全部历史行为人消息均为混合体；conversation_memories 已是语义块纯正文（勿动）
```

## 3. 实施步骤（严格按序）

### Step 0 备份
- DB 导出：参照 `_backup/_memories_backup_20260822.json` 的方式全量导出 character_chat_messages
- 文件备份：`_backup/<当日>/` 收集所有待改文件

### Step 1 后端访问器封装（零风险先行）
- 新增 `get_display_content(msg)` / `get_message_reasoning(msg)`（放 utils 或 models）
- 兼容三形态：新格式（extra 有 reasoning）、存量混存行（content 含 think）、普通行
- 将后端直接读 content 的消费方逐步接入（审计清单见 Task 4.2 于 separate-reasoning-pipeline）

### Step 2 写入侧切换
- `stream_builder.StreamResult.final_text()`：改为返回纯 `full_content`（新增 `final_text_with_think()` 保留旧行为一版本期可不用，或直接删除包裹逻辑）
- websocket.py 两处持久化路径 + character_ext.py SSE 路径（L4580 区域）+ continue/swipe 路径：content 只存正文
- reasoning-only（无正文）沿用现有报错分支不落库
- **必须与 Step 4 前端同一次上线**（中间态会出现前端读不到思考）

### Step 3 存量迁移脚本
- 仿 `_rechunk_memories_semantic.py` 模式：扫 content 含 `` 且 extra 无 reasoning 的行 → 拆分 → content=正文、extra.reasoning=思考 → 默认 dry-run

### Step 4 前端消费点迁移（8 处）
| 文件 | 位置 | 现行为 | 改为 |
|---|---|---|---|
| useCharacterChat.ts | L343/L446/L732/L911/L1264 | 收到 reasoning 主动拼 `` 进内部 content | 分离持有，渲染直读 |
| useChatMessages.ts | L123 | 同上 | 同上 |
| messageUtils.ts | L20 buildAssistantContent | 拼接工具 | 重构或废弃 |
| formatting.ts | L944-960 提取补丁 | 显示时实时提取 | 直读 extra.reasoning（保留提取作存量兜底亦可） |

### Step 5 插件边界适配（唯一风险点的缓冲）
- 传给 iframe 插件（smart-card-runtime frame 通道）的消息内容：先用 `extractReasoningTags` 预剥离，reasoning 走独立字段
- **实测 BubbleDialogue**：加载带思考的历史消息，核对其气泡渲染正常（它是随卡分发插件，源码不可改，靠边界适配保它）

### Step 6 回归验证
- 全量 pytest（基线见 §5）+ 前端 tsc
- 容器重建 healthy
- 真实推理模型对话冒烟：DB content 无 think、extra.reasoning 有值、前端折叠展示正常、BubbleDialogue 渲染正常、存量旧消息显示不变形

## 4. 注意事项与坑

1. **Step 2 与 Step 4 必须同一批上线**——只改后端会让前端在过渡期显示不出思考
2. **formatting.ts 的提取补丁建议保留为兜底**（对未迁移的第三方导入数据仍有效），但主路径切 extra.reasoning
3. **reasoning-only 判定简化**：分离后 `full_content` 不含 think，「无正文」判定更纯粹（现有 EMPTY-RESP-FIX 逻辑不变即可）
4. **swipes 列**（L121 Text）：swipe 备选文本同样混存 think，迁移脚本与写入侧都要覆盖
5. **不要动 conversation_memories**——记忆表已是纯正文语义块，与本任务无关
6. **st-compat/st-native 冻结**照旧
7. PowerShell 引号坑 / pytest 以全量为准 / stash 对照法 —— 见上一份交接文件 §4（仍然全部有效）
8. 测试基线：850~860 过 / 4 存量失败（mvu_engine、p1_fixes、st_contract、st_plugin_import 各 1），零新增失败才算过
9. **【重要】聊天里输出字面尖括号标签会被吞**：在对话中粘贴或输出含 think 等标签的文本时，从开标签起后面的内容会被渲染层当作思考块吞掉（2026-08-23 实测：本项目交接提示词连续两次在「content 列的」处截断，元凶即正文中的字面标签）。**规避**：聊天与提示词中一律用文字描述（如「think 标签」「think 开闭标签包裹体」），绝不写裸标签；需要展示标签时用代码块并确认接收方支持。本文件与各交接文档中的标签均为文字转述，从磁盘读取不受影响

## 5. 验收标准

- [ ] 新消息：content=纯正文、extra.reasoning=思考、无双份存储
- [ ] 存量消息：显示与导出不变形（迁移脚本后 DB 干净）
- [ ] 前端：思考折叠展示正常（show_model_reasoning 开关有效）、BubbleDialogue 正常
- [ ] 插件：iframe 内渲染正常（边界适配生效）
- [ ] 全量测试零新增失败；容器 healthy

## 6. 相关文档索引

- `TODOS.md`「[规划中] 思考/正文分离存储」条目（前置条件三件套）
- `.trae/specs/separate-reasoning-pipeline/tasks.md` 底部架构决策存档
- ST 参照：`SillyTavern-1.18.0/.../scripts/reasoning.js`（五层体系）、`default/content/presets/reasoning/*.json`（模板）
- 上一份交接：`HANDOFF_思考链路与记忆切分实施_2026-08-22.md`（环境坑 §4 仍然全部有效）
