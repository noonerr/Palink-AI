# Palink-AI 前端加载优化计划（已执行版）

更新时间：2026-04-02

## 一、当前打包快照（本次实测）

基于本地与容器内统一构建结果（vite build）：

| 组件 | 文件 | 体积 | gzip |
|------|------|------|------|
| 主包 | index-DjlR19ZL.js | 291.66KB | 91.80KB |
| 搜索库 | find-Bbh4VGuo.js | 174.40KB | 58.68KB |
| Chat 桌面 | ChatViewDesktop-CUBEok1N.js | 17.78KB | 5.47KB |
| Chat 移动 | ChatViewMobile-ByMFNVfd.js | 14.09KB | 5.36KB |
| Character 桌面入口 | CharacterViewDesktop-B7q34NPT.js | 14.23KB | 4.84KB |
| Character 移动入口 | CharacterViewMobile-BU6LSIqg.js | 14.35KB | 4.90KB |
| Character 共享核心 | CharacterChat-DcpCTzRO.js | 347.86KB | 107.05KB |
| Settings | SettingsView-DVR-GKW6.js | 393.69KB | 98.56KB |
| Workspace | WorkspaceView-mwPKAlf_.js | 16.76KB | 4.44KB |
| ChatSessionList 共享块 | ChatSessionList-2X1ypFLK.js | 53.37KB | 17.69KB |
| ChatInput 共享块 | ChatInput-SDi7mT-0.js | 126.66KB | 37.94KB |
| useMobileBottomPadding | useMobileBottomPadding-DVYN3kbj.js | 0.90KB | 0.46KB |

---

## 二、本轮已执行项 ✅

### 1) CharacterView 入口拆分（已执行）

- 新增：frontend/src/components/views/CharacterViewDesktop.tsx
- 新增：frontend/src/components/views/CharacterViewMobile.tsx
- 改动：frontend/src/App.tsx
  - 桌面路由改为懒加载 CharacterViewDesktop
  - 移动路由改为懒加载 CharacterViewMobile

说明：
- 已实现“桌面/移动独立入口文件”。
- 当前构建结果显示仍有大共享块 CharacterChat（359.48KB），说明逻辑层仍高度共用，后续需要进一步按设备拆出渲染层。

### 2) ChatSessionList 去重策略增强（已执行）

- 改动：frontend/vite.config.js
  - 在 manualChunks 中显式指定：
    - ChatSessionList
    - useMobileBottomPadding

说明：
- 目标是避免在复杂路由图下出现重复打包与不稳定分块。

### 3) Settings 重组件延迟加载（已执行）

- 改动：frontend/src/components/views/SettingsView.tsx
  - 将 OCSettings / TokenUsagePanel / ModelEditor 改为 React.lazy + Suspense

说明：
- 功能行为保持一致，构建通过。
- 当前总体体积未显著下降，下一步需按“Tab 文件级拆分”继续优化。

### 4) Settings Tab 文件级拆分（已执行，待继续压实分包）

- 新增：frontend/src/components/views/settings-tabs/AdminDefaultsTab.tsx
- 新增：frontend/src/components/views/settings-tabs/AdminUsersTab.tsx
- 新增：frontend/src/components/views/settings-tabs/AboutTab.tsx
- 新增：frontend/src/components/views/settings-tabs/ProfileTab.tsx
- 新增：frontend/src/components/views/settings-tabs/ModelsTab.tsx
- 改动：frontend/src/components/views/SettingsView.tsx
  - profile / models / admin_defaults / admin_users / about 改为 React.lazy + Suspense 按需挂载

说明：
- 代码结构已完成高频+低频 Tab 拆分，行为验证通过。
- 当前构建产物中 SettingsView 仍较大（393.69KB），后续需继续下沉共享依赖边界。

### 5) ChatSessionList 结构瘦身（第一阶段已执行）

- 改动：frontend/src/components/ui/custom/ChatSessionList.tsx
  - 去除 ScrollArea 依赖
  - 去除内联 <style>，改为原生滚动容器 + Tailwind 移动端底部补白

说明：
- 构建通过，行为保持一致。
- 共享块由 131.95KB 降至 53.37KB，降幅明显，后续继续清理非首屏依赖。

### 6) 构建配置生效路径修正（已执行）

- 改动：frontend/vite.config.js
  - 将此前写在 vite.config.ts 的构建压缩与分包策略同步到实际生效配置

说明：
- 根因是项目存在 vite.config.js 与 vite.config.ts，构建实际使用 js 配置。
- 修正后分包策略与压缩策略已稳定生效。

### 7) 构建与容器验证（已执行）

- 已执行：docker compose up -d --build
- 结果：frontend、backend、db 全部正常启动

### 8) Tailwind 歧义警告治理（已执行）

- 改动：frontend/src/components/ui/custom/Sidebar.tsx
  - 替换 `ease-[cubic-bezier(0.22,0.65,0.22,1)]` 为 `ease-dock-smooth`
- 改动：frontend/src/index.css
  - 新增 `.ease-dock-smooth` 过渡缓动类

说明：
- `npm run build` 与容器内构建均已无该歧义警告。

---

## 三、仍需推进的核心优化 ⏸️

| 方案 | 预期收益 | 风险 | 当前状态 |
|------|----------|------|----------|
| Character 真正设备级拆分（渲染层+依赖层） | 高（目标再降 120~180KB） | 中 | 进行中（已完成入口拆分） |
| Settings 共享依赖继续下沉（重组件/弹窗逻辑） | 中高（目标降 80~140KB） | 中 | 进行中（已完成 Tab 组件化） |
| ChatSessionList 结构瘦身（滚动容器与图标依赖复审） | 中（目标降 30~70KB） | 低中 | 进行中（已完成第一阶段） |

---

## 四、下一轮实施顺序（建议）

1. CharacterView 深拆分（优先级 P0）
   - 将桌面/移动 JSX 与设备特有状态彻底拆入独立文件
   - 仅保留最小共享 hook 层，避免生成大共享块

2. SettingsView 分包边界压实（优先级 P1）
  - 继续将 Provider 编辑弹窗和本地模型管理下沉为按需模块
  - 避免非设置路径提前依赖设置专用模块

3. ChatSessionList 体积治理（优先级 P1）
  - 继续审核运行时图标与列表项逻辑
  - 将非首屏交互逻辑延后注入

4. 构建体积回归监控（优先级 P2）
  - 每次优化后固定采样 `vite build` 与 manifest，避免 chunk 回弹

---

## 五、验收标准

1. 构建必须通过（npm run build 无报错）
2. 容器必须通过（docker compose up -d --build 后服务正常）
3. 聊天、角色、设置、工作区主流程无回归
4. 首屏路径仅加载必要 chunk，不提前拉取非首屏重模块
