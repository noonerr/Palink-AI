# better-sidebar 修改教训（2026-08-15）

## 教训：不要直接重命名/替换 node_modules 里的插件目录
上次我为了修改 dsh-better-sidebar，把原目录改名成 -orig 并建 junction 指向 patched 副本，
结果 GUI 重启后 better-sidebar 从 boot 图消失（装配丢失）。
- 原因：bundle 通道要求 `profiles/web/package.json` 的 `dsh.profile.bundles` 数组包含插件名，
  重命名目录不影响装配，但 GUI 重启后 bundle 扫描失败导致插件丢失。
- 外援修复：把 `dsh-better-sidebar` 加回 bundles 数组（dependencies 里本来就有 ^0.11.0）。
- 教训：修改已安装插件时，先确认装配来源（bundles 数组 / cordis.patch.yml），
  不要动 node_modules 目录结构；直接改源码 + 重新构建 lib 即可，
  浏览器半 HMR 会自动更新，Node 半才需要重启。
- 当前装配状态（外援修复后）：bundles 包含 dsh-better-sidebar，boot 图正常。
