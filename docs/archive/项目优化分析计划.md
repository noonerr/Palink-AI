# Palink-AI 项目优化计划（安全执行版）

计划日期：2026-04-02
当前版本：v0.3.0
执行策略：默认不破坏现有功能，优先做可验证、可回滚、低风险优化

---

## 一、本轮已自动执行

### 1) 后端依赖瘦身（已执行）

执行结果：
- 已从基础依赖中移除重型 ML 包（torch、transformers、sentence-transformers）
- 新增可选依赖文件 `backend/requirements-ml.txt`，仅在需要 SentenceTransformer 时安装

安全说明：
- 默认运行配置为 `MEMORY_EMBEDDING_PROVIDER=fastembed`，与当前生产配置一致
- 兼容模式保留：如未来要启用 sentencetransformer，可显式安装可选依赖

### 2) Docker 多阶段构建（已执行）

执行结果：
- `backend/Dockerfile` 已升级为 builder/runtime 双阶段
- builder 阶段负责编译与安装依赖，runtime 阶段仅保留运行所需系统库
- 新增构建参数 `INSTALL_ML_EXTRAS`，默认 `0`（不开启重型 ML 依赖）

安全说明：
- 保留原有镜像源策略与启动方式
- 通过容器重建验证服务可正常启动

### 3) 前端依赖清理（已执行）

执行结果：
- 已移除未使用的调试依赖 `kimi-plugin-inspect-react`
- 保留 `sharp`，因为 `frontend/generate-icons.js` 存在直接使用

### 4) 前端构建压缩增强（已执行）

执行结果：
- `frontend/vite.config.ts` 已追加安全压缩选项：
  - `pure_funcs: ['console.log', 'console.info']`
  - `passes: 2`
  - `mangle.safari10: true`

安全说明：
- 仅影响生产构建压缩行为，不改变业务逻辑

---

## 二、验证结果

### 构建验证

- 前端 `npm run build` 通过
- 当前关键 chunk 结果保持稳定（无新增异常警告）

### 运行验证

- 已执行 `docker compose up -d --build`
- 服务状态正常：frontend / backend / db 均为 Up

---

## 三、安全与风险处理

### 1) npm audit 结果（已执行，不盲修）

- 扫描结果：11 个漏洞（2 moderate，9 high）
- 未自动执行 `npm audit fix --force`，因为会触发 `vite-plugin-pwa` 破坏性升级，存在功能回归风险

处理策略：
- 先在独立分支做依赖升级回归测试，再合并到主线

### 2) 后端 API 拆分优先级复核

- 实测行数：
  - `backend/app/api/chat.py`：262 行
  - `backend/app/api/character.py`：137 行
- 结论：未达到当前“必须拆分”的阈值，本轮不做结构性拆分，避免引入行为回归

---

## 四、下一轮建议（按风险从低到高）

1. 前端依赖漏洞治理分支
   - 锁定可安全升级范围（先不做 force）
   - 每次升级后执行 build + 关键页面回归

2. Character 与 Settings 的深度按需拆分
   - 继续做设备级与 Tab 级动态加载
   - 目标是进一步降低首屏非必要加载

3. 可选 ML 模式文档化
   - 在部署文档补充 `INSTALL_ML_EXTRAS=1` 的启用方式
   - 明确 fastembed 与 sentencetransformer 的切换流程

---

## 五、本轮实际改动文件

- `backend/requirements.txt`
- `backend/requirements-ml.txt`（新增）
- `backend/Dockerfile`
- `frontend/vite.config.ts`
- `frontend/package.json`
- `frontend/package-lock.json`
- `项目优化分析计划.md`

---

计划更新时间：2026-04-02
