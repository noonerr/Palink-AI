# SPEC — 测试归零批次：三契约漂移甄别修复 + U-3 封存端点级回归

> 日期：2026-08-24
> 并行约束：与安全第二批（N-6/N-7）、性能体验批（N-3/N-9/N-12）**三线并行**。
> **禁改文件清单**（两线占用，本批一律不碰）：
> 后端 `openai_compat.py` / `app/core/security.py` / `main.py`；
> 前端 `uploadUrls.ts` / `MarkdownRenderer.tsx` / `ImageViewer.tsx` / `ImageLightbox.tsx` /
> `ImageGenerationManagementTab.tsx` / `useCharacterChat.ts` / `CharacterChat.tsx` / `Message.tsx`。
> 本批主战场：`backend/tests/` + 必要时 `bridge.js` / `CharacterCardRenderer.tsx` /
> 智能卡导入服务（若需改动命中禁改清单中的文件，停止并在报告中说明，勿强改）。

---

## 目标

宿主全量基线从 **933 passed / 3 failed** 推进到 **全绿（除环境性 skip 外 0 failed）**，
让此后所有批次的验收不再背负"存量失败豁免清单"。

当前三个失败（全部为前端契约类，mvu_engine 已于 2026-08-24 清偿）：

| # | 测试 | 失败摘要 | 初步诊断（施工前须复核） |
|---|------|---------|------------------------|
| F1 | `test_p1_fixes.py::TestP17ExtensionPromptStoreMerge::test_character_card_renderer_registers_get_extension_prompts` | 断言 CharacterCardRenderer.tsx 含 `getExtensionPrompts` 或 `__palink_extension_prompts` | 该文件已被 `scripts/_p1-split.mjs` 机械拆分（文件头自述 AUTO-GENERATED），注册逻辑大概率迁入拆出的子组件文件——断言未跟随。**倾向断言过期**：更新为扫描拆分后的实际落点文件 |
| F2 | `test_st_contract.py::TestBridgeJsRealApiPathsSync::test_palink_owned_endpoints_in_bridge_js` | bridge.js REAL_API_PATHS 缺 20 个端点（live2d-pool 全套、elevenlabs 语音全套、images、summarize、extra/caption+classify 等） | 这些端点在白名单缺失时会被错误代理到 ST sidecar 造成数据分叉——**倾向真缺口**：把 20 个路径补入 bridge.js 的 REAL_API_PATHS（按既有分组格式）；补齐后该测试自然转绿 |
| F3 | `test_st_plugin_import.py::test_import_captures_root_settings_html_as_template` | 根目录 settings.html 未被抽为模板 | 需甄别：导入服务的模板抽取逻辑是否真回退（对照 git log 该断言引入时的行为）或测试夹具过期 |

## 施工要求

### T-1 三失败甄别与处置（F1/F2/F3）

每个失败的处置必须走同一决策树：

```
复现失败 → 读断言意图 → 对照 git 历史（断言引入时的行为）
→ 判定：【A】真回归 → 修产品代码（最小 diff，说明根因）
        【B】断言过期/机械拆分漂移 → 更新断言跟随现状（注释注明判定依据）
        【C】需要改动禁改清单文件 → 停止，报告并等待审计线协调
```

- F2 若确认补白名单：注意 REAL_API_PATHS 是"Palink 自有端点代理白名单"，补入前抽查
  其中 2~3 个端点确实存在于后端路由（防把 ST 自有端点误加进去造成反向代理错乱）
- 所有断言更新的 commit message 与报告必须给出【B】判定的证据链

### T-2 U-3：封存机制端点级回归（补 test_st_mode_normalization.py 或新文件）

现有守卫全是函数级；补 DB fixture 的 HTTP 端点级测试（FastAPI TestClient 即可）：

1. **PUT 重定向**：造一个 user_setting 行 `silly_tavern_mode='st-compat'` →
   `PUT /api/users/me/settings {"silly_tavern_mode":"st-native"}` →
   直查 DB 断言落库值为 `palink-native`
2. **GET 报告重定向且不回写**：DB 存量保持 `'st-compat'` →
   `GET /api/users/me/settings` 返回 `palink-native` → 再查 DB 原值未被改写
3. 认证依赖照抄仓库既有端点测试的 fixture 模式（若无先例，用 dependency_overrides
   注入 get_current_user）

### T-3 边界与红线

- 只允许触碰：`backend/tests/**`、`bridge.js`、`CharacterCardRenderer.tsx`、
  智能卡导入服务相关源码（且不得命中禁改清单）
- **禁改**：禁改清单九文件、`roleplay_prompt_assembly.py`、`character_message_builder.py`、
  `stream_builder.py`、memory_module、任何封存集合值
- 不执行 npm run build、不 git commit、不动容器（审计线统一收尾）
- 宿主全量目标：**933+X passed / 0 failed**（X 为新增测试数）；tsc 干净

## 验收清单（审计线使用）

- [ ] 三个失败逐一给出 A/B/C 判定与证据链；全绿达成
- [ ] F2 若补白名单：抽查端点存在性记录在案
- [ ] U-3 两场景测试过（PUT 落库重定向 / GET 报告不回写）
- [ ] 未命中禁改清单；宿主全量 0 failed；tsc 干净
