# Palink-AI 角色扮演组件与 Silly Tavern 对比分析及改进计划

## 1. 现状分析

### 1.1 当前功能清单

#### 已实现功能：
- ✅ 角色卡管理（创建、编辑、删除）
- ✅ Silly Tavern 角色卡格式支持（PNG/JSON 导入导出
- ✅ 基础聊天对话（第一人称/第三人称模式
- ✅ 记忆压缩功能
- ✅ 会话管理

### 1.2 与 Silly Tavern 的主要差距：

| 功能模块 | 差距描述 | 优先级 |

|-----------|---------|-------|
| 对话分支系统 | 缺失 | 高 |
| 角色动作和表情 | 缺失 | 高 |
| 人物卡模板系统 | 缺失 | 中 |
| 多角色互动 | 缺失 | 中 |
| 场景切换 | 缺失 | 中 |
| 对话历史导入导出 | 缺失 | 中 |
| 记忆管理增强 | 部分实现 | 高 |

---

## 2. 详细改进计划

### 2.1 阶段一：对话分支系统（高优先级）

#### 目标：实现类似 Silly Tavern 的对话分支管理

**技术方案：**

**数据库设计变更：
```python
# 新增数据表结构变更：
- conversation_branches 表：
  - id: 分支 ID
  - session_id: 会话 ID
  - parent_message_id: 父消息 ID
  - branch_name: 分支名称
  - created_at: 创建时间
```

**实施步骤：**
1. **数据库迁移：新增分支相关数据库表
2. **后端 API 开发：**
   - POST /api/character-sessions/{session_id}/branches - 创建分支
   - GET /api/character-sessions/{session_id}/branches - 获取分支列表
   - POST /api/character-sessions/{session_id}/branches/{branch_id}/switch - 切换分支
   - DELETE /api/character-sessions/{session_id}/branches/{branch_id} - 删除分支
3. **前端 UI 开发：**
   - 分支可视化分支选择器组件
   - 分支创建/切换 UI
   - 分支历史视图
   - 分支合并功能
4. **消息渲染更新：**
   - 消息显示分支指示器

**预期效果：** 用户可以保存、回溯和探索不同对话路径

---

### 2.2 阶段二：角色记忆与上下文管理增强（高优先级）

#### 目标：实现角色长期记忆系统

**技术方案：**

**现有记忆模块增强：
1. 语义化记忆分类：
   - 核心记忆（固定不遗忘）
   - 短期记忆（最近对话）
   - 摘要记忆（压缩的历史）
2. 记忆标签系统
3. 记忆重要性评分机制

**实施步骤：**
1. **数据库增强：**
   - 新增 character_memories 表
   - 新增 memory_tags 表
2. **后端 API：**
   - POST /api/memory/{session_id}/memories - 添加记忆
   - GET /api/memory/{session_id}/memories - 获取记忆
   - PUT /api/memory/{session_id}/memories/{memory_id} - 编辑记忆
   - DELETE /api/memory/{session_id}/compress - 智能压缩
3. **前端 UI：**
   - 记忆管理界面
   - 记忆编辑组件
   - 记忆标签管理

**预期效果：** 角色能够记忆关键对话信息，支持手动调整记忆

---

### 2.3 阶段三：人物卡自定义系统增强（中优先级）

#### 目标：增强人物卡属性和模板系统

**技术方案：**

**人物卡字段扩展：**
- 支持完整 Silly Tavern v2 格式完整支持
- 新增属性：性格、外貌、喜欢/不喜欢、关系、世界观等
- 模板系统：预定义模板库
- 可视化编辑器

**实施步骤：**
1. **数据库扩展 Character 模型增强：**
   - 新增更多字段
   - 模板表
2. **后端 API：**
   - GET /api/character-templates - 获取模板
   - POST /api/character-templates - 创建模板
   - PUT /api/characters/from-template - 从模板创建
3. **前端 UI：**
   - 增强的编辑器
   - 模板库
   - 拖拽式可视化编辑

**预期效果：** 更丰富的人物卡属性，模板创建和分享

---

### 2.4 阶段四：交互体验提升（中优先级）

#### 目标：角色动作、表情、场景系统

**技术方案：**

1. **角色动作表情系统：
   - 预设动作库
   - 表情支持
   - 场景描述
   - 快捷回复

**实施步骤：**
1. **数据结构：**
   - 动作表情定义
   - 场景定义
2. **后端支持：
   - 动作表情 API
   - 场景 API
3. **前端渲染：
   - 表情选择器
   - 场景切换器
   - 快捷回复

**预期效果：** 更丰富的角色扮演表现力

---

### 2.5 阶段五：多角色互动（中优先级）

#### 目标：支持多角色同时在线互动

**技术方案：**

1. **多角色会话：
   - 角色关系系统
   - 对话轮次管理
   - 角色间关系影响

**实施步骤：**
1. **数据库：**
   - multi_character_sessions 表
   - character_relationships 表
2. **后端：**
   - 多角色聊天 API
   - 关系管理 API
3. **前端：**
   - 多角色选择器
   - 关系设定 UI

**预期效果：** 支持多角色互动

---

### 2.6 阶段六：导入导出完善（中优先级）

#### 目标：对话历史导入导出

**技术方案：**

1. **对话历史导出：**
   - JSON/文本格式
   - Silly Tavern 兼容
2. **导入：**
   - 支持多种格式

**实施步骤：**
1. **后端：**
   - 导出 API
   - 导入 API
2. **前端：**
   - 导出 UI
   - 导入 UI

**预期效果：** 完美支持对话历史导入导出

---

### 2.7 阶段七：性能优化（低优先级）

#### 目标：长对话性能优化

**技术方案：**

1. **本地缓存：**
   - IndexedDB 本地存储
   - 消息分页加载
   - 虚拟滚动

**实施步骤：**
1. **前端：**
   - 虚拟列表
   - 本地缓存
2. **后端：**
   - 分页 API 优化

---

## 3. 实施路线图

| 阶段 | 功能 | 预计时间 | 优先级 |

|------|------|---------|-------|
| 1 | 对话分支系统 | 1-2 周 | 高 |
| 2 | 记忆系统增强 | 1-2 周 | 高 |
| 3 | 人物卡增强 | 1 周 | 中 |
| 4 | 交互体验提升 | 1 周 | 中 |
| 5 | 多角色互动 | 1-2 周 | 中 |
| 6 | 导入导出 | 1 周 | 中 |
| 7 | 性能优化 | 1 周 | 低 |

**总计：** 约 7-10 周

---

## 4. 技术栈确认

当前技术栈：
- **后端：** FastAPI + SQLAlchemy
- **前端：** React + TypeScript + Tailwind CSS
- **数据库：** SQLite
- **扩展依赖：** 无需新增重大依赖

---

## 5. 风险与注意事项

1. **向后兼容：** 确保所有改动不破坏现有功能
2. **用户体验：** 渐进式改进，保持界面简洁
3. **数据迁移：** 数据库迁移需谨慎
4. **测试：** 每个阶段完成后需充分测试

